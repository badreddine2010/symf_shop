{% extends 'base.html.twig' %}

{% block title %}Images index{% endblock %}

{% block body %}
    <h1>Images index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Produit</th>
                <th>Catégorie</th>
                <th>Link</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for image in images %}
            <tr>
                <td>{{ image.id }}</td>
                <td>{{ image.product.name}}</td>
                <td>{{ image.product.category.name }}</td>
                <td>						<img src="{{ asset('build/images/' ~ image.link) }}"  alt="...">
</td>
                <td>
                    <a href="{{ path('app_images_show', {'id': image.product.id}) }}">show</a>
                    <a href="{{ path('app_images_edit', {'id': image.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_images_new') }}">Create new</a>
{% endblock %}
